<h4>Add an Application </h4>

   <%= form_for [user, registered_applications] do |f| %>
      <div class="form-group">
         <%= f.label :appname %>
         <%= f.text_field :appname, class: 'form-control', placeholder: "Enter Application Name" %>
         <%= f.number_field :url, class: 'form-control', placeholder: "URL", value: nil, required: true %>
      </div>
      <div class="form-group">
         <%= f.submit "Save", class: 'btn btn-success' %>
      </div>
   <% end %>
